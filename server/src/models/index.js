/**
 * 模型导出文件
 */

const User = require('./User');
const Room = require('./Room');
const RoomPlayer = require('./RoomPlayer');
const GameRound = require('./GameRound');
const PlayerCard = require('./PlayerCard');
const PlayRecord = require('./PlayRecord');
const RoundScore = require('./RoundScore');

module.exports = {
  User,
  Room,
  RoomPlayer,
  GameRound,
  PlayerCard,
  PlayRecord,
  RoundScore
};
